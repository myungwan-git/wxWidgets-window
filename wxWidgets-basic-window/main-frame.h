#pragma once

/*
// 이 헤더는 일반적으로 사용되는 대부분의 헤더가 포함되어 있다.
// 이 말은? 그만큼 용량이 클것이다. 이 안에 frame.h 도 존재한다.
// 따라서 frame.h 안의 클래스를 상속받아 구현할 수 있다.
#include <wx/wx.h>

// frame include
#include <wx/frame.h>

class MainFrame : public wxFrame {
	private:
		void OnButtonClicked(wxCommandEvent &event);
	public:
		// Window title name argument.
		MainFrame(const wxString &title);
		void printExamm();

};
*/